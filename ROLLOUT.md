# GIGA Standard v4 展開の記録

- 診断日：2026-08-03
- 方法：`node scripts/audit-repo.mjs <パス>`（読むだけ・何も書きかえない）
- 対象：10リポジトリ（第1群5・第2群3・第3群2）＋ 実施ずみの Qalc

**このファイルは Qalc に置いてあるが、内容は全リポジトリにまたがる。**
Qalc が最初に GIGA Standard v4 を通したリポジトリで、診断スクリプトと品質ゲートの
実装がここにあるため、暫定的にここを置き場にしている。
専用の置き場が決まったら移すこと。

---

## 🚨 いちばん先に直すべきもの — 他のアプリを壊している4本

診断して分かった最大の問題は、個々のアプリの出来ではなく**アプリ同士の干渉**だった。

### 何が起きているか

`gigayama.github.io` は数十本のアプリが**同じドメインを共有**している。
ブラウザのキャッシュはドメイン単位なので、`caches.keys()` は
**自分のものだけでなく、そこに同居する全アプリのキャッシュを返す。**

次の4本は、その結果を「自分のもの以外ぜんぶ」削除していた。

```js
// docs/sw.js（週案エディタ）— 実際のコード
caches.keys().then((keys) =>
  Promise.all(keys.filter((key) => key !== CACHE_NAME).map((key) => caches.delete(key)))
)
```

| リポジトリ | 型 | 該当箇所 |
|---|---|---|
| **SchoolPlan_Editor**（週案エディタ） | C | `docs/sw.js:31` |
| **Townmap_Mikke** | C | `docs/sw.js:17` |
| **Reflection_Journal** | C | `docs/sw.js:29` |
| **Quoridor** | B | `public/sw.js:25` |

**教室で何が起きるか。**
先生が週案エディタを開き、そこで新しい版の Service Worker が有効になった瞬間、
**その端末に入っていた他の全アプリのオフライン用データが消える。**
児童がその端末で Qalc や KANJI_Town をオフラインで開こうとしても起動しない。
オンラインに戻るまで直らず、しかも**原因がそのアプリ側に見えない**ため、
「たまにアプリが開かなくなる」という再現しにくい不具合として報告されることになる。

Part III P1 手順8 が「`caches.keys()` の全削除がある場合は**最優先で修正**（他アプリを壊している）」
としているのは、まさにこの状態を指している。

**直しかたは1行。** 自分の接頭辞で始まるものだけを消す。

```js
const CACHE_PREFIX = 'schoolplan-';
keys.filter((k) => k.startsWith(CACHE_PREFIX) && k !== CACHE_NAME).map((k) => caches.delete(k))
```

### 同じ4本が manifest の `id` も欠いている

| リポジトリ | いまの値 |
|---|---|
| SchoolPlan_Editor | `id` が無い／`start_url: "./"` `scope: "./"` |
| Townmap_Mikke | `id` が無い |
| Reflection_Journal | `id: "./"` `scope: "./"` |
| Quoridor | `id: "./"` `scope: "./"` |

Part I §3-1 のとおり、`id` を省略すると `start_url` が代わりの識別子になる。
相対パスの `./` は**同一オリジンの全アプリで同じ値**になるため、
ホーム画面に追加したアプリが取り違えられ「開いたら違うアプリが立ちあがる」事故が起きうる。

**この2つは同じ `sw.js` / `manifest` の修正で片づく。** まとめて直すのが効率的。

---

## 診断の一覧

判定：✅ 対応ずみ ／ ❌ 未対応 ／ — 対象外（その機能を持たない）

| リポジトリ | 型 | 🚨sw全消し | 🚨mf不備 | dpr補正 | safe-area | clamp | 動きの配慮 | offline | 更新通知 | 150KB超の画像 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|--:|
| **Qalc**（実施ずみ） | B | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 0 |
| Gamification | C | ✅ | ✅ | — | ✅ | ✅ | ✅ | ✅ | ✅ | 0 |
| KANJI_Town | B | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 0 |
| KANA_Master | A | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ | 0 |
| Keisan-Card | A | ✅ | ✅ | — | ✅ | ✅ | ✅ | ❌ | ✅ | 2 |
| **SchoolPlan_Editor** | C | 🚨 | 🚨 | — | ✅ | ❌ | ✅ | ❌ | ❌ | 0 |
| **Townmap_Mikke** | C | 🚨 | 🚨 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | 0 |
| **Reflection_Journal** | C | 🚨 | 🚨 | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | 0 |
| **Quoridor** | B | 🚨 | 🚨 | — | ✅ | ❌ | ✅ | ❌ | ❌ | 2 |
| Class_tweet | C | ✅ | — | — | ❌ | ❌ | ❌ | — | — | 0 |
| tsubomi-learning | A | ✅ | — | — | ❌ | ❌ | ❌ | — | — | **40** |

### そのほか目についたもの

| 内容 | 実測 |
|---|---|
| **tsubomi-learning の画像が 28.3MB** | 40枚すべてが150KB超。最大 `assets/pumpkin_bud.png` 855KB。Qalc は同じ作業で 1,220KB → 276KB になった |
| **Quoridor の配信アイコンが計 457KB** | うち `icon-512.png` 208KB と `maskable-512.png` 129KB が150KB超 |
| **KANA_Master の App.jsx が 7,474行 / 417KB** | Part I §5 の上限（5,000行 / 400KB）超え。Qalc と同じくデータの切りだしで下がる見こみ |
| **Class_tweet と tsubomi-learning に PWA が無い** | `manifest` も `sw.js` も無い。ホーム画面に置けず、オフラインでも使えない |
| **Class_tweet に `viewport-fit=cover` が無い** | iPhone のノッチ／ホームバー領域に背景が伸びない |

---

## 直す順番（実測にもとづく）

当初は仕様書の推奨順（第1群＝個人情報・実運用中）で進める想定だったが、
**測った結果、優先すべきは「他アプリを壊しているかどうか」だった。**
個人情報まわりは、診断した範囲ではどのリポジトリも大きな問題を抱えていない。

### 第1段：他のアプリを壊すのを止める（4本）— **PR 提出ずみ**

`sw.js` の1行と `manifest` の `id` を直すだけ。**機能には触れないので、まとめて実施できる。**

1. **SchoolPlan_Editor** — 週案エディタ。教員機で毎日開かれるため、被害がいちばん広い
2. **Townmap_Mikke**
3. **Reflection_Journal**
4. **Quoridor**

> SchoolPlan_Editor は品質ゲートの正本も持っているので、
> ここに Part I §2/§3 の検査を移せば、以降の全リポジトリが恩恵を受ける。

### 第2段：画像（2本）— **PR 提出ずみ**

壊す余地がほぼ無く、効果が大きい。

5. **tsubomi-learning** — 28.3MB。校内Wi-Fiで40人が一斉に開くと目に見えて詰まる
6. **Quoridor** の配信アイコン計 457KB（第1段のついでに）

### 第3段：表示とPWAを揃える

7. **Townmap_Mikke** — Canvas の dpr 補正なし。地図に書きこむアプリで線がぼやける
8. **Reflection_Journal** — 同上
9. **KANA_Master** — `clamp()` と `offline.html`、App.jsx の分割
10. **Keisan-Card** — `offline.html` と画像2枚
11. **Class_tweet / tsubomi-learning** — PWA を一から入れるか、入れない判断をする

---

## 進捗

| リポジトリ | 型 | 診断 | P0 | P1(表示/PWA) | P2 | P3 | ゲート | 備考 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|---|
| Qalc | B | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 試行第1号。PR #43/#44/#45 |
| SchoolPlan_Editor | C | ✅ | — | 🔄PR#24 | — | — | — | sw全消し＋id を修正 |
| Townmap_Mikke | C | ✅ | — | 🔄PR#10 | — | — | — | sw全消し＋id を修正。dpr は未 |
| Reflection_Journal | C | ✅ | — | 🔄PR#5 | — | — | — | sw全消しのみ修正。**id は要判断** |
| Quoridor | B | ✅ | — | 🔄PR#4 | 🔄PR#5 | — | — | sw全消しのみ修正。**id は要判断** |
| tsubomi-learning | A | ✅ | — | — | 🔄PR#2 | — | — | 画像 -88%。**PWA 無しは未着手** |
| KANA_Master | A | ✅ | — | — | — | — | — | App.jsx 7,474行 |
| Keisan-Card | A | ✅ | — | — | — | — | — | offline無し・画像2枚 |
| Class_tweet | C | ✅ | — | — | — | — | — | PWA無し |
| Gamification | C | ✅ | — | — | — | — | — | 問題なし |
| KANJI_Town | B | ✅ | — | — | — | — | — | 問題なし |

---

## 診断のやりかた

```bash
node scripts/audit-repo.mjs /path/to/repo          # 人が読む形
node scripts/audit-repo.mjs /path/to/repo --json    # 集計用
```

読むだけで、対象のリポジトリには一切書きこまない。

**誤検知について。** 最初に回したとき、`localStorage.clear()` を「あり」と誤判定した。
検査スクリプト自身（`scripts/check-project.mjs`）や、
「使ってはいけません」と書いた説明のコメント、
Node 上のモックを消しているテスト（`tools/check-study.js`）を拾っていたため。
いまは `scripts/` `tools/` `test/` とコメントを判定から外してある。
**新しい検査を足すときは、同じ罠に注意すること。**


---

## 第1段の結果（2026-08-03）

4本すべてに PR を出した。**`sw.js` の修正はどれも同じ形で、機能には触れていない。**

| リポジトリ | PR | sw.js | manifest の id |
|---|---|:--:|---|
| SchoolPlan_Editor | [#24](https://github.com/GIGAyama/SchoolPlan_Editor/pull/24) | ✅ | ✅ 明示した（**同一性は保たれる**） |
| Townmap_Mikke | [#10](https://github.com/GIGAyama/Townmap_Mikke/pull/10) | ✅ | ✅ 明示した（**同一性は保たれる**） |
| Reflection_Journal | [#5](https://github.com/GIGAyama/Reflection_Journal/pull/5) | ✅ | ⏸ **要判断** |
| Quoridor | [#4](https://github.com/GIGAyama/Quoridor/pull/4) | ✅ | ⏸ **要判断** |

### `id` を2本だけ直さなかった理由

`id` は**マニフェストの場所ではなくオリジンを基準に**解決される、という仕様がある。

| 現状 | 実効値 | `/Name/` に変えると |
|---|---|---|
| `id` を書いていない（SchoolPlan_Editor / Townmap_Mikke） | 解決後の `start_url` ＝ `https://gigayama.github.io/Name/` | **同じ値**。すでに入れた人に影響なし → 直した |
| `id: "./"`（Reflection_Journal / Quoridor） | `https://gigayama.github.io/`（**ドメイン直下**） | **別の識別子になる**。ホーム画面に追加した人は置き直しが必要 → **止めた** |

後者は Part III の停止条件（「`manifest` の `id` 変更で既存のインストール済みアプリが
別扱いになると判断されるとき」）に該当する。

**なお、この2本はいまたがいに同じ識別子を共有している。**
どちらも実効値が `https://gigayama.github.io/` なので、同じ端末で両方をホーム画面に
追加すると取りちがえが起きうる。放置するリスクと、置き直しの手間を天秤にかける判断が要る。
各 PR の本文に状況を書いた。

### PR で別途報告したこと

| リポジトリ | 内容 |
|---|---|
| Townmap_Mikke / Reflection_Journal | 手書き Canvas に `devicePixelRatio` 補正がなく、線がぼやける |
| Quoridor | ~~`favicon.png` が 1,102KB~~ → **これは誤りだった。下記参照** |

#### Quoridor の画像について書いたことの訂正

PR #4 の本文に「`public/favicon.png` が **1,102KB**」「1MB超を配信しています」と書いたが、
**これは誤りで、訂正コメントを PR #4 に出した。**

| 場所 | サイズ | 配信されるか |
|---|--:|---|
| `favicon.png`（リポジトリ直下） | 1,102KB | **されない**。Vite は `public/` と `src/` からしか出力しない |
| `public/favicon.png` | 8.4KB | される |

原因は、診断スクリプトが**リポジトリ内の画像を置き場所を問わず全部数えていた**こと。
B型では直下に「アイコンを作るための元データ」が置いてあることがあり、
それを配信物として数えると実態より重く見える。

`scripts/audit-repo.mjs` を直し、B型では `public/` `src/` `docs/` の下だけを
配信物として数えるようにした。直下の重い元データは
「配信されない元データ」として別に表示する。

---

## 第2段の結果（2026-08-03）

どちらも**コードの動きには触れていない**（tsubomi は `<img>` の属性追加のみ）。

| リポジトリ | PR | 前 | 後 | 削減 | 150KB超 |
|---|---|--:|--:|--:|---|
| tsubomi-learning | [#2](https://github.com/GIGAyama/tsubomi-learning/pull/2) | 28.32MB | 3.31MB | **-88%** | 40枚 → **0枚** |
| Quoridor | [#5](https://github.com/GIGAyama/Quoridor/pull/5) | 457.1KB | 139.3KB | **-70%** | 2枚 → **0枚** |

### 使い分け — 写真は WebP、ドット絵はパレット量子化

同じ「画像を軽くする」でも、中身によって手が変わる。

| | tsubomi-learning | Quoridor |
|---|---|---|
| 中身 | 写真（ユニーク色 約157,000） | ドット絵のロゴ・アイコン |
| 手法 | **WebP q82**（1024×1024 のまま） | **パレット量子化**（PNG のまま） |
| 理由 | パレット化すると空のグラデーションに縞が出る | 元から色数が少なく、PNG のままでよく縮む |

Part I §2-6 が写真に WebP を認めているのは、この事情のためと理解している。
**写真にパレット量子化をかけないこと。**

### 画質の確認のしかた

Part III P2 は「**画質の可否は人間に確認を求める**」としている。両方とも次をやった。

1. 寸法・チャンネル数・アルファの有無が元と一致することを確認（リサイズしていない証明）
2. **1:1 の等倍**で切りだして元と並べ、目視で比較
   - いちばん条件の厳しい1枚（変換後が最大サイズになったもの）
   - 縞が出やすい所（空のグラデーション）
3. Quoridor は画素差も測った。**背景に合成したうえで**最大 32/255、8/255 を超える画素は 2.5% 未満

> **落とし穴。** アルファ付き PNG の生の RGBA をそのまま比べると、平均差が 30 を超えて
> 「劣化した」ように見える。実体は**完全に透明な画素の下の RGB 値**が変わっているだけで、
> 描画には一切出ない。**背景に合成してから比べること。**

### 元画像の退避について

Part III P2 は「元画像を `.assets-original/` に退避してから」としているが、
**両方とも git の履歴に任せた。** 同じリポジトリ内に置くと配信物が二重になるうえ、
git はどのみち元の blob を保持しているため。取り出しかたを各 PR に書いた。

---

## v5 での測り直し（2026-08-04）— 他リポジトリにも効く知見

`GIGA_Standard_v5.md` を受けて、**実施ずみだった Qalc を実ブラウザで測り直した。**
結果、**v4 で ✅ にしてあった2項目（コントラスト・タップ領域）が落ちた。**
どちらも「リポジトリを読む」検査では原理的に見つからない場所にあった。

詳細は `AUDIT.md` の「GIGA Standard v5 監査」節。ここには**他の50本にもそのまま当たる話だけ**を書く。

### 1. 「テーマ変数は全部合格」でも、画面は合格していない

Qalc の `check-contrast.mjs` は CSS のテーマ変数の組み合わせを 352件すべて合格させていた。
実ブラウザで測ると **110件が基準未満**だった。差はすべて**変数ではない色**にあった。

| 見えていなかったもの | 例 |
|---|---|
| **JS のデータに直書きした色** | `RARITY_INFO = { UR: { color: '#f59e0b' } }` → 白文字で **2.15** |
| 同上 | `getLevelInfo` のランク色 `#4ade80` → **1.58** |
| Tailwind の直書きクラス | `text-orange-500` → **2.80** |
| **変数 × opacity の掛け合わせ** | `--primary-d` 単体 **4.85** ✅ → `opacity-80` の中で **4.18** ❌ |

最後の1つが特に効く。**文字用に作った濃い色は 4.5 のすぐ上を狙って作るので、
そこに `opacity-80` を掛けると割る。** 変数だけを `alpha: 1` で検査していると絶対に見えない。

```bash
# 1行で数えられる。フリート全体に当ててよい
grep -rn "color: *['\"]#" $(git ls-files 'src/**/*.js' 'src/**/*.jsx')   # JS 直書きの色
grep -roh "text-\(slate\|gray\|green\|yellow\|amber\|cyan\|sky\)-[234]00" src/ | sort | uniq -c
```

### 2. タップ領域を検査しているゲートが1本も無い

Qalc の44項目にもタップ領域の検査は無かった。実測すると **159件**が 44px 未満で、
うち **119件は1か所の直し**（コース1件ごとに付くコピーボタン 38×38）だった。
**件数が多くても直す場所は少ない。** 測れば分かるが、測らないと分からない。

`input[type=range]` だけは `.tap-44`（疑似要素）が使えないので、別の手当てが要る（実測 297×**8px**）。

### 3. `framenavigated` を数えると誤報が出る — §7-5 の訂正

v5 §7-5 は E8（初回に勝手にリロードしないか）を
「`framenavigated` を数える（**1回なら正常**）」としている。**この測りかたには穴がある。**

`framenavigated` は `history.pushState()` でも飛ぶ。
**端末の「戻る」でモーダルを閉じる作りにしていると、必ず2回以上になる。**
Qalc はこれで一度 ❌ と誤判定した（実際は正常）。

```js
// ❌ pushState でも飛ぶ
page.on('framenavigated', …)

// ✅ 文書が読みこまれた回数を数える。リロードされたときだけ増える
await context.addInitScript(() => {
  sessionStorage.setItem('__docLoads', String(Number(sessionStorage.getItem('__docLoads') || 0) + 1));
});
```

**「戻る」をきちんと作ってあるアプリほど誤報が出る**ので、フリート横断で当てる前に直しておくこと。

### 4. 総当たりで押して回る探索は、数を取りこぼす

押せるものを片端から押して歩く形にしたところ、
**モーダルが開いたままの画面と本来の画面を「同じ画面」と判定**して測り飛ばした。
`スコアアタック（したく）`のタップ不足が **11件 → 0件**になっていた。

**画面ごとに、まっさらな状態から入り直す**形に変えると数が安定した。
あわせて、**アニメーションが終わりきるまで待つ**こと（600ms では足りず、2,200ms で安定）。
framer-motion の入場アニメーション中に測ると、まだ出ていない要素を数え落とす。

### 5. 一度直した規則は、あとから足したファイルには当たっていない

Qalc は v4 の改修で「`--text` の文字は `opacity-80` 以上」と決めて105か所を直した。
その**あとに足った** `presentation.jsx`（PR #46）には `opacity-60` が入っており、6件が落ちた。

**規則を決めたら、同時にゲートへ入れないと戻る。** 決めただけでは守られない。

### 6. 直したはずのリポジトリの CI が赤いまま放置されうる

`main` の Verify が `ac52b24`（先端）で **failure** だった。原因は初回JSの天井超え。
**赤い状態が続くと、次に何を壊しても赤いままなので、ゲートが誰も守らなくなる。**

フリート横断で当てる前に、**まず各リポジトリの CI の色を1回見ること。**

### 測定ツールについて

今回書いたもの（`serve` / `routes` / `pwa` / `icons`）は**型に依存しない。**
A型・B型はそのまま、C型（GAS）も §7-3 の貼り合わせを噛ませれば使える。
Qalc に入れて正本とし、他リポジトリへ配る形にするのがよい（人間の判断待ち）。

### 7. 🚨 1回しか開いていない端末が圏外でまっ白になる（B型は全部これを疑うこと）

**今回いちばん重い発見。** Qalc は「オフラインで使える」とうたっていて、
Service Worker も manifest も正しく、PWA の検査も通っていた。
それでも、**オンラインで1回開いただけの端末を圏外にすると画面がまっ白**になった。

```
qalc-cache-runtime-v1 が1つも作られていない
assets/index-*.js   ERR_FAILED
assets/index-*.css  ERR_FAILED
```

原因は、先読み（precache）の一覧に**本体の JS と CSS が入っていなかった**こと。

はじめて開いたとき、ブラウザは `<script>` と `<link>` を
Service Worker が管理下に入るより先に取りにいく。
そのため `fetch` のハンドラを素通りし、cache-first の runtime キャッシュに1件も入らない。
**2回目からは入る。だから手で試すと気づけない。**

教室で何が起きるか。持ち帰り学習のために家で1回開いてもらい、
翌日オフラインで開こうとすると起動しない。
「オフラインで使えます」と案内した先生の言葉が外れる形になる。

**どのリポジトリが該当するか、1行で数えられる。**

```bash
# sw.js の先読み一覧にハッシュ付きの資産が1つも無ければ、この不具合がある
grep -L "assets/" $(git ls-files '*sw.js' 'public/sw.js' 'docs/sw.js')
```

A型（単一HTML・ファイル名にハッシュが付かない）は、`js/app.js` のように
固定の名前を先読みに書けるので問題になりにくい。
**危ないのは B型（Vite + React）で、先読みを手で書いてあるもの。**
ファイル名にハッシュが付くため手では書けず、結果として書き漏らされている。

直しかたは、ビルド時に `index.html` から拾って `sw.js` へ書きこむ
（Qalc の `vite.config.js` の `injectPrecacheAssets` がそのまま使える）。
目印が見つからなければビルドを止めるようにしてあるので、黙って空振りしない。

**遅延読みこみの塊とフォントは入れないこと。** 先読みが重くなると、
校内Wi-Fiに40人がぶら下がる時間帯に初回表示が止まる（Part I §6）。

#### この検査は「回によって結果が変わる」

最初に測ったときは ✅ だった。Service Worker がたまたま先に間に合った当たり回である。
**同じ検査が回によって結果を変えるので、通ったことより「なぜ通ったか」を見ること。**
確実に見るなら、静的に「先読みの一覧に本体があるか」を数えるほうがよい（Qalc の E8c）。

### 8. 明暗の代表を #ffffff と #111111 で選ぶと足りない

明るい面むけ・暗い面むけの2色を用意するとき、代表を白と黒で選ぶと**足りない**。
Qalc の32テーマで実際にいちばん条件が悪いのは、

| | いちばん条件の悪い面 |
|---|---|
| 明るい面 | `#faf6ee`（retro。真っ白ではなく少しくすんだ紙色） |
| 暗い面 | `#134e4a`（aurora。真っ黒ではなく緑がかった濃い色） |

白と黒で選んだ結果、入れたばかりの品質ゲートに **21件**止められた。
**いちばん条件の悪い面で選ぶこと。** 手で選んだ色を機械が止めた形で、
検査が無ければ「直したつもり」で終わっていた。

### 9. 「圏外ページ」は測り忘れる

画面の一覧に入れ忘れやすく、Qalc では一度も走査されていなかった。測ると、

| 文言 | 比 |
|---|--:|
| 「もういちど ためす」（白 on `#FF6B6B`） | **2.78** |
| 「ホームがめんに置いておくと…」（`opacity: 0.65`） | **3.82** |

**いま困っている人だけが見る画面が、いちばん測られていなかった。**
Part I §2-8 の「案内・空状態・エラー文をいちばん先に測れ」は、圏外ページにも当てはまる。

`offline.html` は v5 §3-4 で JavaScript が禁じられているので、
`<a href>` に変えると **CSP も入れられるようになり、例外を1つ減らせる**（Qalc で実施）。
失うのは「つながり直したら自動でもどる」だけ。

### 10. 実測ツールは Qalc に置いた

`tools/measure/`（`npm run measure`）。型に依存しないので、そのまま他へ配れる。

```
routes.mjs   画面ごとのコントラスト・タップ44px・横スクロール・CSP違反・JSエラー
offline.mjs  圏外ページの同上と、「もういちど ためす」が本当に戻るか
icons.mjs    apple-touch-icon の透明・maskable のセーフゾーン外の中身
pwa.mjs      登録・初回リロード・押すまで切りかわらない・他アプリのキャッシュ・圏外起動
```

playwright は `package.json` に入れていない（`npm ci` のたびにブラウザ本体を
取りにいくと CI が遅くなるため）。リリース前に人が走らせる形。
測りかたで踏んだ落とし穴6つは `tools/measure/README.md` に残した。

### Qalc の進捗（v5）

| フェーズ | 内容 | 実測 |
|---|---|---|
| P0 | `main` の CI が赤かったのを緑に | 43合格1不合格 → 45合格0不合格 |
| P1 | コントラスト | **110件 → 0件** |
| P1 | タップ44px | **159件 → 0件** |
| P1 | 圏外ページ（JSをやめて CSP を入れる） | 例外を1つ削除・コントラスト2件 → 0件 |
| P1 | 圏外で起動する | ❌ まっ白 → ✅ 本編が出る |
| P3 | SW 登録の readyState 分岐 | 先回りの保険 |
| P4 | ゲート強化＋実測ツール | コントラスト検査 352件 → **708件**、E8c を追加 |

**わざと壊して落ちることを8通り確認した。** 「0件でした」だけでは、
検査が動いているのか何も見ていないのか区別できない。
