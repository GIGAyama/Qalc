# GIGA Standard v4 監査：Qalc（カルク ～目指せ、計算マスター～）

- 監査日：2026-08-03
- 対象コミット：`main` 相当（作業ブランチ `claude/rollout-bul4o6`）
- 判定した型：**B型（Vite + React）** — `vite.config.js` あり、`base: '/Qalc/'` 設定済み
- 実測方法：リポジトリ全文の grep と `npm ci && npm run build` による実ビルド。推測値は含めない。

判定記号：✅ 適合 ／ ⚠️ 部分適合 ／ ❌ 未対応 ／ — 対象外（理由を明記）

---

## 総評

**このリポジトリは、すでにかなりの水準にある。** CSP は meta で投入済み、Service Worker は
自アプリ接頭辞のキャッシュだけを掃除しており（他アプリを壊していない）、フォントは自己ホスト、
`localStorage.clear()` も `postMessage(..., '*')` も無い。学習ログ `study.v1` も仕様どおりで、
`pagehide` による確定保存・5分の離席ルールも実装されている。

不足しているのは主に次の4点に集約される。

1. **LICENSE の実ファイルが無い**（README に一文があるだけ）
2. **PWA の「インストール導線」と「オフライン時の顔」が無い** — `beforeinstallprompt` の捕捉、
   アプリ内インストールボタン、更新通知、`offline.html` がいずれも未実装
3. **手書きキャンバスに devicePixelRatio 補正が無い** — Chromebook の高DPI機で線がぼやける
4. **画像が重い** — favicon 239KB / icon-512 283KB / icon-maskable-512 177KB（上限の4〜8倍）

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ❌ | ファイルが存在しない。README.md:312 に「## ライセンス / © Qalc GIGA山」の記載のみで、許諾条件が示されていない |
| A2 | .gitignore | ✅ | 1,169 バイト。`node_modules/` `dist/` を含む |
| A3 | dependabot.yml | ✅ | `.github/dependabot.yml` あり。npm=weekly（production / development をグループ分け）、github-actions=monthly |
| A4 | README.md / MANUAL.md 両方 | ⚠️ | README.md（26,876 バイト・全13節）は非常に充実。**MANUAL.md（先生向け）が無い** |

補足：`git ls-files` に `.clasp.json` / `.env` は無い（秘密情報のコミットなし）。

---

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（connect-src が最小） | ✅ | index.html:31 に meta で投入済み。`connect-src 'self' https://0.peerjs.com wss://0.peerjs.com` の2件のみ。ワイルドカードなし。`object-src 'none'` `frame-src 'none'` `form-action 'none'` まで閉じている |
| B2 | 秘密情報・IDの直書きなし | ✅ | APIキー・スプレッドシートID・メールアドレスの直書きを検出せず |
| B3 | OAuthスコープ最小 | — | GAS を使わない B型のため対象外 |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage(..., '*')` の該当0件 |
| B5 | サーバー側5段ガード | — | サーバーを持たない P2P（PeerJS）構成のため対象外。代替として `src/roomAccess.js`（267行）にリーダー承認制の入室制御と受信データ検証があり、専用テスト `scripts/roomAccess.test.mjs` で担保されている |

---

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | GAS 非使用のため対象外 |
| C2 | 自動復旧（シート再生成） | — | 同上 |
| C3 | pagehide で記録確定 | ✅ | App.jsx:3582-3590。`beforeunload` が bfcache 経路で発火しない件のコメント付き |
| C4 | 通信失敗時のリトライと明示 | ⚠️ | `retry` / `reconnect` に相当する自動再接続処理が見当たらない。校内Wi-Fiが混むと「へや」から落ちたまま戻らない可能性 → **要調査（P3 で扱う）** |
| C5 | localStorage.clear() を使っていない | ✅ | 該当0件。`study.records.v1` は保護されている |

---

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | index.html:33。`user-scalable=no` は付いていない（拡大可能＝a11y 上も適切） |
| D2 | 100dvh を使用（100vh 単独でない） | ✅ | `100vh` 単独の使用0件。`h-[100dvh]` を App.jsx:4030, 5537 で使用 |
| D3 | safe-area-inset を適用 | ⚠️ | 2箇所のみ（App.jsx:4030 と LearningTools.jsx:2360、いずれも `padding-bottom`）。**左右パディング（横向き時のノッチ欠け）と上部ヘッダー（App.jsx:5540 の `sticky top-0`）に未適用** |
| D4 | clamp() による fluid type | ❌ | `clamp(` の該当0件。文字サイズは Tailwind の固定クラス（`text-2xl` 等）のみ。1366×768 の Chromebook と 375px のスマホで同じ値が出る |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ❌ | **`devicePixelRatio` の該当0件。** `HandWritingCanvas`（App.jsx:2341-2426）はバッファを CSS px と1:1（`parent.clientWidth/Height`）で確保しており、DPR 2 の Chromebook・iPad では線と数字がぼやける。`ResizeObserver` と描画コード自体は既に整っているため、補正の挿入だけで済む |
| D6 | 320px 幅で横スクロールが出ない | ⚠️ | 静的解析では断定不可。**P1 の検証で実測する** |
| D7 | 画像に width/height、150KB以下 | ❌ | `<img>` は TerritoryBattle.jsx:409 の1箇所のみで `width`/`height` なし（CLS 要因）。150KB 超が3ファイル：favicon.png 239KB / icon-512.png 283KB / icon-maskable-512.png 177KB |
| D8 | コントラスト 4.5:1 以上 | ⚠️ | テーマが16種（App.jsx:5500-5516）あり全組み合わせの機械判定は未実施。各テーマは `--text` を濃色・`--bg` を淡色に取る設計で概ね良好だが、`aurora` `hanabi` `ninja` 等の暗色テーマは要実測 |
| D9 | タップ領域 44px 以上・touch-action | ⚠️ | `body { touch-action: manipulation }`（App.jsx:5521）✅、キャンバスは `touch-none` ✅、消去ボタンは `w-11 h-11`=44px ✅。**`-webkit-tap-highlight-color` の指定0件／`overscroll-behavior: contain` の指定0件**（引っぱり更新の暴発を防げていない） |
| D10 | prefers-reduced-motion 対応 | ⚠️ | index.css:52-57 で画面ゆれ4種の keyframes のみ無効化。**framer-motion（全画面で使用）・canvas-confetti・`navigator.vibrate`（App.jsx:56-92 で9種類）は無効化されない。**感覚過敏の児童向けの設定 OFF スイッチも無い |
| D11 | 提示モード（一斉授業で使う場合） | ❌ | `requestFullscreen` / 拡大表示の該当0件。ボスバトル・じんとりバトルはクラス全体で行う機能のため、本来は必要 |
| D12 | 印刷CSS | — | 印刷を前提とした機能（ワークシート・記録の配布）を持たないため対象外。「かんがえるどうぐ」は画面上の一時的な補助表示であり、印刷用途ではない |

---

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | `"id": "/Qalc/"` `"scope": "/Qalc/"` `"start_url": "/Qalc/"` — **同一オリジン共有の事故は起きていない**（最重要項目をクリア） |
| E2 | アイコン4種 + apple-touch-icon | ✅ | 192 / 512 / maskable-192 / maskable-512 / apple-touch-icon の5つとも実在 |
| E3 | beforeinstallprompt を head 最上部で捕捉 | ❌ | **該当0件。** イベント自体を受け取っていないため、Chrome の合図を毎回取りこぼしている |
| E4 | インストールボタンをアプリ内に設置 | ❌ | インストール導線・`display-mode: standalone` の判定ともに0件 |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | public/sw.js:25-34。`CACHE_PREFIX = 'qalc-cache-'` で絞り込み済み。他アプリを壊していない |
| E6 | sw.js が localStorage に触れていない | ✅ | 該当0件 |
| E7 | 更新通知（あたらしいバージョンがあります） | ❌ | main.jsx:19-23 は `register()` するだけ。`updatefound` の監視も `SKIP_WAITING` の受信ハンドラも無い。新版を出しても児童の端末は旧版のまま |
| E8 | offline.html | ❌ | ファイルが存在しない。圏外時は `caches.match('/Qalc/index.html')` に頼るのみで、それも外すと白画面になる |
| E9 | APP_VERSION を今回のリリース値に更新した | ⚠️ | `CACHE = 'qalc-cache-v3'`。**今回の変更に合わせて v4 へ上げる必要がある** |
| E10 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ❌ | MANUAL.md 自体が無い |

### E 追加所見（Part I §3-1 / §3-3 との差分）

| 箇所 | 内容 |
|---|---|
| manifest.webmanifest | `display_override` と `launch_handler` が未設定。二重起動時に既存ウィンドウへ寄せられない |
| public/sw.js:19 | `cache.addAll(SHELL)` は**1本でも失敗すると全体が落ちる**。個別 `cache.add()` + `catch` に分ける必要がある |
| public/sw.js:47 | navigate 失敗時のフォールバックが `index.html` のみ。`offline.html` の二段目が無い |
| public/sw.js | `message` イベントによる `SKIP_WAITING` 受信ハンドラが無い（E7 と対）|
| public/sw.js | 静的アセットが `CACHE`（版付き）に混ざる。版を上げるたび全アセットを取り直す |

---

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live | ⚠️ | `aria-label` 14件・`alt` 1件（`<img>` が1つなので網羅）・**`aria-live` 0件**（正解／保存完了が読み上げられない）・**`role="dialog"` 0件**（モーダルが複数あるが未指定） |
| F2 | キーボードのみで全機能に到達 | ⚠️ | `focus-visible` 相当の指定が1件のみ。手書きパネルにはキーボード代替（数字入力）が存在するが、全画面の Tab 順は未検証 |
| F3 | 初回JS 300KB以下（gzip前） | ❌ | **`dist/assets/index-*.js` = 846.6KB（gzip 246.7KB）。** 単一チャンクで、Vite も 500KB 超の警告を出している |
| F4 | 1ファイル 5,000行 / 400KB 以内 | ❌ | **`src/App.jsx` = 5,610行 / 374KB。**行数が上限超過。次点は LearningTools.jsx 2,418行 / 128KB（適合） |

### F 追加所見（性能）

| 指標 | 目標 | 実測 | 判定 |
|---|---|---|:--:|
| 初回 JS（gzip前） | 300KB 以下 | 846.6KB | ❌ |
| 初回 CSS（gzip前） | — | 321.6KB（gzip 101.4KB） | ⚠️ |
| 総アセット（初回） | 1MB 以下 | 約1.4MB（HTML 3KB + JS 847KB + CSS 322KB + favicon 244KB + フォントサブセット数本） | ❌ |
| dist 全体 | — | 9.1MB（うちフォント woff2 が 6.7MB） | 参考値 |
| woff（旧形式）の残存 | 0 | 0件（`dropLegacyWoff` プラグインが効いている） | ✅ |

フォント 6.7MB は `unicode-range` によるサブセット分割済みで、画面に出た文字ぶんだけが落ちてくる。
初回の実効転送量には含まれないため、これ自体は問題ではない。

---

## G. 学習ログ（study.v1）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `src/studyLog.js`（ロジック版1.1・74行）。`localStorage['study.records.v1']` に保存のみ、外部送信なし。氏名・出席番号・メールを持たない。上限500件・items 200件・`wrong` の文字列サニタイズあり。壊れた JSON からの復帰も実装済み |
| G2 | 中断記録・5分ルール | ✅ | `src/studySession.js:239` `STUDY_ABORT_AWAY_MS = 5 * 60 * 1000`（5分。4分では締めない）。`IDLE_MS = 60 * 1000` で無操作時の `activeMs` 加算停止（§2.8 準拠）。テスト `scripts/studyLog.test.mjs` あり |

---

## 対応フェーズの割り当て

| フェーズ | 対象 | 破壊リスク |
|---|---|:--:|
| **P0 — 法務** | A1（LICENSE 新規作成） | なし |
| **P1 — 表示・PWA** | D3（safe-area 左右・上部）／D5（**Canvas DPR 補正**）／D9（tap-highlight・overscroll）／D10（framer-motion・confetti・振動の reduced-motion 対応）／E3・E4（インストール導線）／E7（更新通知）／E8（offline.html）／E9（v3→v4）／sw.js の §3-3 準拠化／manifest の display_override・launch_handler | 小〜中 |
| **P2 — 性能** | D7（favicon・icon-512・icon-maskable-512 の圧縮、`<img>` に width/height） | 小（**画質の可否は人間に確認**） |
| **P3 — 保守性** | A4（MANUAL.md 作成）／C4（再接続の調査）／F1・F2（aria-live・role="dialog"・Tab順）／D4（clamp() 導入）／**F3・F4 は提案のみ**（App.jsx 5,610行の分割・コード分割は合意なしに実施しない） | 中 |
| **P4 — 品質ゲート** | `scripts/check-project.mjs` + `quality.config.json` を追加し、`npm run check` を CI に組み込む | なし |

---

## 実施後の状態（2026-08-03 追記）

`/rollout` で P0〜P4 をすべて実施した。品質ゲート `npm run check` の 41 項目は全て合格。

| 監査時 ❌/⚠️ | いま | どう直したか |
|---|:--:|---|
| A1 LICENSE なし | ✅ | MIT を追加 |
| A4 MANUAL なし | ✅ | `MANUAL.md` を新規作成（うまくいかないとき／iOS 手順を含む） |
| C4 再接続 | 📋 | 自動再接続は入れず、理由とともに README に明記（下記） |
| D3 safe-area 一部 | ✅ | ヘッダー上端・フッター下端・左右に適用 |
| D4 clamp() なし | ✅ | `tailwind.config.js` の fontSize を clamp() に |
| **D5 Canvas の DPR 補正なし** | ✅ | 手書きとQRコードの両方を dpr 倍で描画（実測 2.00 倍） |
| D7 画像が上限の4〜8倍 | ✅ | 1,220KB → 276KB。150KB 超は0枚 |
| D9 tap-highlight / overscroll | ✅ | `src/index.css` に追加 |
| D10 reduced-motion が一部 | ✅ | framer-motion・紙ふぶき・振動まで対応＋アプリ内の切りかえ |
| D11 提示モードなし | ✅ | 拡大・全画面・名前かくし・演出オフを実装 |
| E3/E4 インストール導線なし | ✅ | `public/pwa-install.js`＋アプリ内ボタン（iOS は手順案内） |
| E7 更新通知なし | ✅ | 待機を検知して「あたらしい バージョンが あります」 |
| E8 offline.html なし | ✅ | 追加。つながり直したら自動でもどる |
| E9 APP_VERSION | ✅ | v3 → v4 |
| E10 iOS 手順なし | ✅ | MANUAL に記載 |
| F1 aria-live 0件 | ✅ | 正誤の読み上げ・トースト・モーダルの role |
| **F4 App.jsx 5,610行** | ✅ | データを `src/data/` に分離 → **3,881行 / 253KB** |
| **F3 初回JS 802KB** | ⚠️ | 遅延読みこみで **646KB（gzip 197KB）**。目標 300KB には未達（下記） |
| lint が動いていない | ✅ | `.eslintrc.cjs` を追加し CI に組みこみ。エラー0件 |

### CSS の内訳（追加調査）

`index-*.css` が 325KB（gzip 100KB）あり、初回描画をブロックしていた。中身を測ったところ：

| 中身 | 生 | gzip | 割合 |
|---|---:|---:|---:|
| `@font-face` 366件（3ウェイト × 122サブセット） | 277KB | 91KB | **85%** |
| Tailwind ほか | 48KB | 9KB | 15% |

Tailwind ではなく**フォントの定義そのもの**が原因だった。
`@font-face` は1件あたり平均757バイトで、その大半が `unicode-range` の長い16進リスト。

フォントの CSS を静的 import から動的 import に変え、Tailwind と別のファイルに分けた。

| | before | after |
|---|---:|---:|
| 描画をブロックする CSS | 325KB（gzip 100KB） | **48KB（gzip 8.9KB）** |

サブセット分割そのものは残してある。減らせば `@font-face` は3件で済むが、
このアプリは**先生が作ったオリジナル問題や CSV 取りこみで任意の漢字が出る**ため、
サブセットを削ると豆腐（□）になる危険がある（Part I §2-7 の但し書き）。

あわせて `--font-ui` にフォールバックの連鎖を定義した（Part I §2-7）。
これまでは `'Zen Maru Gothic', sans-serif` だけで、届くまでのあいだ角ゴシックで出て、
届いた瞬間に丸ゴシックへ大きく字形が変わっていた。
`Hiragino Maru Gothic ProN`（iPad）・`Kosugi Maru`（Android）を先に置いたので、
端末に丸ゴシックがあれば差しかわりがほとんど分からない。

### F3 初回JS（2026-08-03 追記：バトルを遅延読みこみに）

「みんなであそぶ」専用のボスバトル／じんとりバトルは、1人であそぶ児童には1バイトも
要らないのに初回に配っていた。ただし GameView が `useRaidDebuffs` / `useRaidShake` /
`useTerritoryMood` の3つのフックを**無条件に**呼ぶため、まるごと遅延にはできない。
フックは条件つきで呼べないからである。

そこで各ファイルを「決まりごと・計算（フックを含む）」と「画面」に分けた。

| ファイル | 役割 | 読みこみ |
|---|---|---|
| `src/battles/raidLogic.js`（293行） | ボスの強さ・技の抽選・ダメージ計算・フック | 最初から |
| `src/battles/territoryLogic.js`（390行） | 盤面の定義・ぬりのコスト・スペシャルの抽選・フック | 最初から |
| `src/BossBattle.jsx`（664行） | ボスのドット絵・技のカットイン・結果発表 | へやを作る／入るとき |
| `src/TerritoryBattle.jsx`（571行） | 盤面・インクの演出・結果発表 | 同上 |
| `src/battles/lazy.jsx` | 画面16個の遅延読みこみと先読み | — |

先読みは `loadPeer()` と同じ場所で呼ぶ。バトルが始まってから取りにいくと
最初の1問が出るまで待たされるため。

| | before | after |
|---|---:|---:|
| 初回JS | 649.2 KiB | **598.1 KiB**（gzip 184KB） |

**残る差は 598KB − 300KB = 298KB。** 内訳は `react-dom` 130KB、`framer-motion` 約120KB、
`src/App.jsx` の画面まわり。ここから先はライブラリを入れかえるか、画面ごとに
チャンクを分けるかになる。`quality.config.json` の `initialJsBytes` は
実測に合わせて下げること。

#### 切りだしたロジックの検証

決まりごとを別ファイルへ動かしたので、値が変わっていないことを
`scripts/battleLogic.test.mjs`（`npm run test:battle`）で確かめている。
ボスの体力・ダメージの上限90・技の分類・テンキーの並びかえの再現性、
盤面の180度対称・ぬりのコスト・スペシャルが盤面の外に出ないことなど。

**この環境では実際の2端末での対戦は確認できていない。**
サンドボックスから PeerJS のシグナリング（`wss://0.peerjs.com`）へ出られないため。
確認できたのは次まで:

- 初回にバトルの画面のチャンクを読んでいないこと
- 「みんなであそぶ」を押すと2つのチャンクと peerjs を取りにいくこと
- 2つのチャンクがブラウザで評価でき、画面の部品8個ずつを正しく export していること
- コンソールエラー0件

**先生の環境で、ブラウザのタブを2つ開いてボスバトルとじんとりバトルを
1回ずつ通していただけると確実です。**

### D8 コントラスト（実測ずみ・一部修正）

`npm run check:contrast` で全32テーマ × 8通り = 256件を実測した（推測ではない）。

**本文はもともと健全だった。** `--text` on `--bg` / `--panel` は 32テーマすべて合格で、
最小でも 7.15:1 ある。問題は「補足の文字に `opacity` をかけている箇所」に集中していた。

| | 修正前 | 修正後 |
|---|---:|---:|
| 合格 | 192 | **229** |
| 4.5:1 未満（3:1 以上） | 97 | 0 |
| 3:1 未満 | 63 | 27 |

計算すると **opacity 77% で全32テーマが 4.5:1 を満たす**（いちばん厳しいのは matcha の背景）。
そこで切りのよい **80%** を下限とし、文字色に `--text` を使っている 105か所の
`opacity-30/40/50/60/70` を `opacity-80` に上げた。
`bg-[var(--text)] opacity-50`（塗りつぶし）や装飾の透過には手を出していない。
**配色は1色も変えていない。**

#### 残っていた27件 — 「面用と文字用の2段階」で解消（2026-08-03 追記）

落ちていた27件はすべて `--primary` / `--secondary` / `--accent` を**文字の色**に使っている箇所だった。
Part I §2-8 が名指ししているとおり、1つの色を「面」と「文字」の両方に使っていたことが原因。

> 明るい色をそのまま文字色に使わない。**面用と文字用の2段階を用意する**

そこで全32テーマに文字用の変種を足した。**色みは変えず、明るさだけを動かしている。**

| 変数 | 用途 | 例（きほんテーマ） |
|---|---|---|
| `--primary` | 面・帯（ボタンの塗りなど） | `#FF6B6B` |
| `--primary-d` | 文字・アイコン | `#e80000`（白背景で 4.6:1） |
| `--secondary` | 面 | `#4ECDC4` |
| `--secondary-d` | 文字・アイコン | `#24817b`（同 4.6:1） |
| `--on-accent` | `--accent` の面に載る文字 | もとの `--text` で足りていればそのまま |

`--on-accent` は、そのテーマ本来の文字色が 4.5:1 を満たしていればその値をそのまま使う。
32テーマ中30は変化なしで、足りなかった `cyber`（白 on `#0ff0fc` = 1.41:1）と
`dark`（`#e2e8f0` on `#f59e0b` = 1.74:1）の2つだけが濃い色になる。

ソース側も `text-[var(--primary)]` → `text-[var(--primary-d)]` に置きかえた（primary 71件 /
secondary 17件 / on-accent 28件）。`bg-[var(--primary)]` などの**面はそのまま**。

| | 修正前 | 修正後 |
|---|---:|---:|
| 文字の組み合わせ | 27件が不足 | **すべて合格** |

#### 塗りつぶしたボタンの上の文字も直した

上を直す過程で、これまで測っていなかった組み合わせが見つかった。
**`--panel`（ほぼ白）の文字を、明るい塗りのボタンに載せている箇所。**
`--primary` の塗りは32テーマ中17、`--secondary` は9しか合格していなかった。

同じ考えかたで `--on-primary` / `--on-secondary` を用意し、**塗りの色は変えずに文字だけ濃くした。**

| 塗り | 変更前（白文字） | 変更後 |
|---|---:|---:|
| `#FF6B6B`（きほん・primary） | 2.78:1 | **6.80:1** |
| `#4ECDC4`（きほん・secondary） | 1.93:1 | **9.76:1** |
| `#f59e0b`（tropical） | 2.15:1 | **8.79:1** |
| `#eab308`（gold・royal） | 1.92:1 | **9.85:1** |

もとの文字色で足りているテーマはそのまま。足りない38件だけを濃い色にした。
`#111111` で届かない中間色の3件（retro の `#c25953` など）は `#000000` を使う。
差しかえは26か所（`bg-[var(--primary)]` と `text-[var(--panel)]` が同じ要素にあるものだけ）。

**結果：352件すべて合格（不足0・要確認0）。**

#### 品質ゲートへの組みこみ

`npm run check` に含めた。いまはすべての組み合わせに文字用の色があるので、
**1件でも落ちていたら直しわすれ**として CI を止める。

| 落ちた場所 | 直しかた |
|---|---|
| 本文と補足 | `opacity` を 80% 以上にする |
| 強調の文字 | `--primary-d` / `--secondary-d` を使う |
| `--accent` の上 | `--on-accent` を使う |
| 塗りの上の文字 | `--on-primary` / `--on-secondary` を使う |

**テーマを足すときは、これらの変数も一緒に足すこと。**

### 残っている項目

| # | 内容 | 扱い |
|---|---|---|
| F3 | 初回JS 646KB（目標 300KB） | 上記のとおり。天井を設けて肥大化は止めてある |
| D8 | 暗色テーマの薄い文字 | 実測して報告のみ（配色は変更しない） |
| C4 | みんなであそぶの自動再接続 | 入れない判断。理由は README「制限とクォータ」 |
| F2 | Tab 順の全画面検証 | 主要導線は確認済み。全画面の網羅は未実施 |

---

## 人間に判断を仰いだこと（回答済み）

1. **ブランチ運用** — → 1ブランチ上でフェーズごとにコミットを分けた（環境の制約による）。

2. **D11 提示モード** — → 実装することで合意。ヘッダーに追加した。

3. **F3・F4 の扱い** — → 分割まで実施することで合意。F4 は解消、F3 は未達（上記）。

4. **D8 コントラスト** — → 報告のみに留めた（上記）。


---

## 他リポジトリとの突き合わせ（2026-08-03 追記）

`add_repo` の許可を得て、Part III P3・P4 が「正本と揃える」としていた2件を実施した。

### 品質ゲートの正本（`SchoolPlan_Editor/scripts/lib/project-quality.mjs`）

読んで突き合わせた結果、**正本と今回の `check-project.mjs` は守備範囲がほとんど重なっていない。**

| | 正本（SchoolPlan_Editor） | 今回（Qalc） |
|---|---|---|
| 想定する型 | **C型（GAS）** | **B型（Vite + React）** |
| 主な検査 | `appsscript.json` の必須ファイル、`.gs` と HTML 内 script の構文、HTML include の解決、OAuth スコープ、X-Frame | manifest / sw.js / offline.html、`100dvh`・safe-area・`clamp()`・Canvas の DPR、画像の大きさ、初回JS、study.v1 |
| 共通 | 秘密情報の直書き、マージの跡、1ファイルの行数・バイト数、`postMessage(..., '*')` | 同左 |

つまり**どちらかがもう一方を置きかえるものではない。**
正本は「GAS のアプリが壊れずに動くか」、今回のものは「Part I §2/§3（表示とPWA）を満たしているか」を見ている。

そこで、**型によらず共通の部分だけを正本に合わせた。**

- 秘密情報の検出パターンを正本の `detectSecretCandidates` と同じにした
  （Google APIキー・GitHub のトークン・OpenAI のAPIキー・秘密鍵）
- マージの跡（`<<<<<<<`）の検出を追加（正本の `MERGE_CONFLICT_MARKER`）

**逆向きの提案**：正本側にも Part I §2/§3 の検査が無い。
`SchoolPlan_Editor` も Chromebook と iPad で使う以上、`100dvh`・safe-area・Canvas の DPR は
同じように要る。今回の `check-project.mjs` の D／E 節を正本へ持っていくとよい（別作業）。

### `studyLog.js` の正本

3本を突き合わせた。

| リポジトリ | 形 | ロジック版 |
|---|---|---|
| Qalc | ESM（`export function saveStudyRecord`） | 1.1 |
| KANJI_Town | ESM（同上） | 1.1 |
| Keisan-Card | IIFE（`global.StudyLog = {...}`） | 1.1 |

**版ずれは無かった。** Qalc と KANJI_Town はコメント以外まったく同一で、
Keisan-Card は書き方が違うだけで振る舞いは同じ（上限500件・設問200件・
`schema: 'study.v1'`・誤答12文字・外部送信なし、すべて一致）。

書き方が違うのは**アプリの型に合わせた必要な差**である。
A型（単一HTML）は ESM の `import` が使えないため IIFE にするしかない。
したがって「1つのファイルを配り直す」形の正本化はできない。

**正本は「ロジック版1.1の振る舞い」と定義し、ESM 版と IIFE 版の2つを公式な形とするのが実態に合う。**
版ずれを見つけられるよう、`check-project.mjs` に検査を足した（全文比較はしない）。

- **G3** … `studyLog.js` にロジック版が明記されているか
- **G4** … 変わってはいけない値（上限500件・設問200件・`study.v1`・誤答12文字）が保たれているか

### まだ着手していないもの

**第1群（個人情報・実運用中の15リポジトリ）への展開。**
Part III「バッチ運用」は、まず破壊リスクの低い1件で P0〜P1 と検証まで通し、
PR の粒度と所要時間を人間と合意してから展開せよ、としている。
**今回の Qalc がその1件目にあたる。**（`AUDIT.md` → 10コミット → PR 2本 → 品質ゲート44項目）

粒度と所要時間をご確認のうえ、どのリポジトリから進めるかご指示ください。
進めるときは `ROLLOUT.md` に進捗を記録する。

---
---

# GIGA Standard v5 監査：Qalc（2026-08-04）

- 監査日：2026-08-04
- 対象コミット：`ac52b24`（`main` の先端）／作業ブランチ `claude/rollout-mk8tlb`
- 前回（v4）との違い：**v5 で新しく求められた項目を、実ブラウザで測り直した。**

v4 の監査は「リポジトリを読む」と「ビルドしてバイト数を測る」で組み立ててあった。
v5 §7 は、それでは足りないと名指ししている。

> **読むだけでは分からないことが多すぎる。**

そこで今回は、ビルドした `dist/` を本番と同じ `/Qalc/` の下に置いて配り、
**Chromium を実際に起動して、画面を押して歩きながら測った。**
結果として、**v4 の監査で ✅ にしてあった項目が2つ落ちた。**
どちらも「読んでも分からない」形をしている。

## 測りかた（再現手順）

```
npm ci && npm run build
node serve.mjs dist 4180          # dist を /Qalc/ の下に配る
node routes.mjs                   # 画面ごとに、まっさらから入り直して測る
node pwa.mjs                      # 登録・更新・オフライン・他アプリのキャッシュ
node icons.mjs                    # アイコンを画素で数える
```

- 端末の想定：**375×667（iPhone SE）** と **320×568（設計の下限）** の2通り、`devicePixelRatio` 2
- 色は 1px 実際に塗って `getImageData` で読む（§7-2。`oklch()` や `color-mix()` を読み違えないため）
- グラデーションの地は色の停留点をすべて展開し、**いちばん不利な組み合わせ**で判定する
- 絵文字（フォント自身の色で描かれる）と、使用不可の要素は WCAG の対象外として除く
- 画面が切りかわった直後ではなく、**アニメーションが終わりきってから測る**（後述の落とし穴）

### 測れた画面（13）

さいしょの画面／ていじモードのせってい／せってい／スコアアタック（したく）／
タイムアタック（したく）／サドンデス（したく）／きせかえ／きせかえ→ガチャ／
コース管理／へやをつくる／へやに入る／**もんだいの画面**／**がくしゅうどうぐ**

### 測れなかったもの（「未計測」であって ✅ ではない）

| 画面・項目 | なぜ測れないか |
|---|---|
| きろく（学習の記録） | 記録が無い状態では出ない。データを仕込めば測れる |
| みんなであそぶ の本編 | `wss://0.peerjs.com` へ出られない（作業環境の制約）。**へやをつくる／入る の入口までは測った** |
| じんとりバトル／ボスバトル | 対戦相手が要る。`TEAMS` の色は静的に計算した（下表） |
| ランク色のうち かけだし 以外 | Lv.1 では出ない。**色は静的に計算した**（下表） |
| 本番（`gigayama.github.io`）での挙動 | プロキシ 403。引き継ぎどおり到達できない |

---

## 🚨 いちばん先に直すもの — `main` の CI が赤い

| 実行 | コミット | 結果 |
|---|---|---|
| Verify（push: main） | `ac52b24`（現在の先端） | **failure** |
| Verify（push: main） | `9b0a027` | **failure** |

原因は品質ゲートの1項目。

```
❌ F3  初回JS が 615.2KB 以下 — 666.3KB（Part I の目標は 300.0KB）
```

PR #46 で提示モード（`presentation.jsx` 234行）と PWA まわり（`pwa.jsx` 215行）が入り、
**前回設けた天井 615.2KB を 51.1KB 超えた。**

これは「JS が重い」こと自体より、**ゲートが赤いまま放置されていること**が問題である。
赤い状態が続くと、次に何を壊しても赤いままなので、ゲートが誰も守らなくなる。
**他のどの修正よりも先に片づける。**

> 合格 43 / 不合格 1。落ちているのは F3 だけで、ほかの43項目は通っている。

---

## v5 で新しく求められた項目の判定

| 節 | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| §6 | **CDN から取る実行コードが 0 バイト** | ✅ | `@babel/standalone` 0件／`cdn.tailwindcss.com` 0件／`unpkg` `jsdelivr` `cdnjs` 0件。React も framer-motion もフォントも同梱 |
| §2-13 | **CSP を入れたうえで動作確認** | ✅ | 全13画面で `securitypolicyviolation` **0件**。`script-src 'self'` のまま動く |
| §2-14 | SRI と版の固定 | — | 外部から読む資産が1つも無い＝対象外 |
| §3-2 | **apple-touch-icon に透明が無い** | ✅ | 180×180／完全に透明 **0%**／半透明 **0%** |
| §3-7 | **maskable のセーフゾーン外の中身 0.2%以下** | ✅ | 192・512 とも **0.00%**。四隅まで下地が伸びている（縮んで見えない） |
| §3-6 | SW 登録の `readyState` 分岐 | ⚠️ | 分岐は無いが、`main.jsx` の**モジュール最外**で呼んでいるので現状は動く（実測で登録を確認）。React の effect へ移した瞬間に黙って壊れる形 |
| §4 | **`rt` の色を決め打ちしていない** | ✅ | `rt { color: … }` の指定なし＝親から継ぐ。実測した rt の**最小比 4.85** |
| §2-1 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` ともに 0件 |
| §1 | 320px で横スクロールが出ない | ✅ | 320×568 の全11ルートで **0件** |
| §7-5 | Service Worker が登録されている | ✅ | `getRegistration()` → active、scope `/Qalc/` |
| §7-5 | **初回訪問で勝手にリロードしない** | ✅ | 文書の読みこみ **1回**（下の「落とし穴」参照） |
| §7-5 | 更新は押すまで切りかわらない | ✅ | 版を上げて `update()` → **3秒放置しても `waiting` のまま** |
| §7-5 | 押したら切りかわる | ✅ | `static-v4` が消え `static-v5-test` になった |
| §7-5 | **他アプリのキャッシュを巻きこまない** | ✅ | `townmap-static-v3` と `keisan-card-runtime-v1` を置いてから版を上げ、**両方とも残った** |
| §7-5 | 圏外で起動する | ✅ | `setOffline(true)` で再読みこみ → 本編が出た |
| §7-5 | 本体が無ければ `offline.html` が出る | ✅ | キャッシュの `index.html` を消してから圏外 → 出た |
| §3-4 | **`offline.html` が JS に頼らない** | ❌ | インラインの `<script>` で「もういちど ためす」を動かしている（後述） |
| §2-8 | **コントラスト（実ブラウザで全画面走査）** | ❌ | **110件**。原因は4つ |
| §2-9 | **タップ領域 44px（`::after` 込みで実測）** | ❌ | **159件**。原因は5つ |

---

## ❌ コントラスト — 110件（原因は4つ）

**v4 の監査では「352件すべて合格」と書いてあった。それは嘘ではないが、足りていなかった。**
`check-contrast.mjs` は `src/App.jsx` に書いてある**テーマ変数の組み合わせだけ**を計算する。
実際に画面に出ている色には、テーマ変数ではないものが混ざっていた。

| # | 原因 | 件数 | 比 | 出どころ |
|---|---|--:|--:|---|
| 1 | **レアリティのバッジ**（塗りの上の白文字） | 97 | 2.15〜3.96 | `src/data/shop.js:297` `RARITY_INFO` |
| 2 | **提示モードの ON/OFF ラベル**（`opacity-60`） | 6 | 3.86 | `src/presentation.jsx:229` |
| 3 | **ランクの色**（`かけだし` ほか） | 4 | **1.58** | `src/App.jsx:427` `getLevelInfo` |
| 4 | **`--primary-d` を `opacity-80` の中で使っている** | 3 | 4.18 | `src/App.jsx:1085` ほか |

### 1. レアリティのバッジ — 97件（いちばん多い）

`きせかえ`（ショップ）と`ガチャ`の全品にレアリティのバッジが付く。塗りの上に白文字を載せている。

| レアリティ | 塗り | 白文字 | 黒文字（`#111111`）にすると |
|---|---|--:|--:|
| N | `#9ca3af` | **2.54** | 7.44 |
| R | `#3b82f6` | **3.68** | 5.13 |
| SR | `#a855f7` | **3.96** | 4.77 |
| UR | `#f59e0b` | **2.15** | 8.79 |

**4色とも基準に届いていない。** 文字は 8〜9px の `font-black` なので、大きな文字の緩和（3:1）も使えない。
Part I §2-8 の「明るい色をそのまま文字色に使わない／面用と文字用の2段階」がそのまま当てはまる。
**塗りの色は変えず、載せる文字を濃くすれば4色とも一度に解ける。**

### 2. 提示モードの ON/OFF ラベル — 6件

```jsx
<span className="ml-1 text-[10px] opacity-60">{checked ? 'ON' : 'OFF'}</span>
```

v4 の改修で「`--text` の文字は `opacity-80` 以上」という下限を決め、105か所を直した。
**`presentation.jsx` はそのあと（PR #46）に足されたので、その下限が当たっていない。**
一度直した形が、あとから足したファイルで戻る典型である。`opacity-80` にすれば足りる（3.86 → 5.6）。

### 3. ランクの色 — かけだしが 1.58

```js
{ min: 0, text: "かけだし", badge: "🌱", color: "#4ade80" }
```

| ランク | 色 | 白地での比 |
|---|---|--:|
| 計算神 | `#9333ea` | 5.38 ✅ |
| 計算マスター | `#ca8a04` | **2.94** |
| 達人 | `#06b6d4` | **2.43** |
| 上級 | `#eab308` | **1.92** |
| 中級 | `#6b7280` | 4.83 ✅ |
| 初級 | `#f97316` | **2.80** |
| **かけだし** | `#4ade80` | **1.58** |

7つのうち **5つが基準未満**。しかも **`かけだし` は Lv.1 のランク**である。
つまり**アプリを初めて開いた児童が最初に見る文字が、いちばん読めない。**
Part I §2-8 が「案内・空状態・エラー文をいちばん先に測れ」と言っているのと同じ形で、
**いちばん困っている人に向けた表示がいちばん読みにくい。**

（`計算マスター` 〜 `上級` は Lv.20 以上でないと出ないため実ブラウザでは未到達。**色だけ静的に計算した。**）

### 4. `--primary-d` が `opacity-80` の中で落ちる — 3件

これは**ゲートが構造的に見えない形**なので、別に書く（下の「品質ゲートの穴」）。

### 参考：測れた範囲で見つかった、そのほかの薄い文字

| 文言 | 場所 | 色 | 比 | 要 |
|---|---|---|--:|--:|
| `?`（答えを打つ欄の合図） | `src/App.jsx:2347` | `--text` を `opacity-20` | **1.46** | 3 |
| ブロックの数（オレンジ） | がくしゅうどうぐ | `text-orange-500` | 2.80 | 3 |
| ブロックの数（水色） | がくしゅうどうぐ | `text-sky-500` | 2.77 | 3 |

`?` は Part I §2-8 が名指ししている例（**「ID 入力欄のプレースホルダ `-` … ここに番号を打つ、という合図」**）と
まったく同じ形で、「ここに答えを打つ」という合図そのものが読めない。

---

## ❌ タップ領域 44px — 159件（原因は5つ）

疑似要素（`::after`）で広げてある分と、チェックボックスを囲む `<label>` の大きさも数えたうえでの結果である。

| # | 原因 | 件数 | 実測 | 出どころ |
|---|---|--:|---|---|
| 1 | **コース管理の「共有コードをコピー」ボタン** | 119 | **38×38** | `src/App.jsx:2718` |
| 2 | **学年フィルタ（1年〜6年・すべて・その他）** | 32 | 55.7〜80 × **40〜42** | `src/App.jsx:1074, 1624` |
| 3 | **「もどる」** | 4 | 297×**36** / 287×**28.8** | したくの画面・へやに入る |
| 4 | **「ぜんぶ えらぶ」** | 3 | 103.4×**28** | したくの画面 |
| 5 | **時間のスライダー** | 1 | 297×**8** | `src/App.jsx:1085, 1650` |

さらに、もんだいの画面から`がくしゅうどうぐ`を開いて測った分（上の159には含めない）：

| 原因 | 実測 | 出どころ |
|---|---|---|
| 「とじる」ボタン | **36×36** | `src/LearningTools.jsx:1824` |
| ブロックのマス | **36×36** | `src/LearningTools.jsx:29` |
| どうぐの切りかえタブ | 106×**36** | `src/LearningTools.jsx` |

**1の119件が突出しているのは、コース1件ごとに1つ付いているため**で、直すのは1か所でよい。
Part I §2-9 のとおり、**ボタン自体を大きくすると詰めて組んであるツールバーが折り返して別の破綻を生む。**
`.tap-44`（疑似要素で当たり判定だけ広げる）を当てるのが正しい。
ただし**スライダー（`input[type=range]`）には疑似要素が使えない**ので、ここだけは別の手が要る。

---

## ❌ `offline.html` が JavaScript に頼っている

v4 では「CSP を入れると『もういちど ためす』が動かなくなる」ため、
`offline.html` だけ CSP を入れず、インラインの `<script>` を残す判断をしていた
（`quality.config.json` の `offline-html-no-csp` として明示的に許可してある）。

**v5 §3-4 はこれを認めていない。**

> **この画面では外部資産にも JavaScript にも頼らない。**
> …再読み込みは `<a href>` で行う（インライン `onclick` は CSP に引っかかる）。

`<a href="/Qalc/">` にすればスクリプトは要らなくなり、**その結果 CSP も入れられる**ので、
いま明示的に許可している例外を1つ減らせる。

**ただし1つ失う。** いまは `online` イベントで「つながり直したら児童が押さなくても自動でもどる」ようにしてある。
JS をやめるとこれは無くなる。**v5 の求めを取るか、この気づかいを取るかは人間の判断としたい。**

---

## 品質ゲートの穴（他リポジトリにも効く）

**「43項目が通っている」ことと「壊れていない」ことは違った。** 落ちた110件と159件は、
すべて既存のゲートが**構造的に見えない場所**にあった。

| 見えていなかったもの | なぜ見えないか |
|---|---|
| `RARITY_INFO` / `getLevelInfo` / `TEAMS` の色 | `check-contrast.mjs` は**テーマ変数の組み合わせだけ**を計算する。JS のデータに直書きした色は範囲外 |
| `text-orange-500` などの直書きクラス | 同上 |
| **`--primary-d` を `opacity-80` の中で使う** | 変数どうしは `alpha: 1` で検査している。**変数は合格でも、掛かった opacity ぶんは見ていない** |
| タップ領域 44px | 検査そのものが無い。**押せるものの大きさは、実際に描かせないと分からない** |

3つめが特に厄介である。

| | 比 |
|---|--:|
| `--primary-d` `#e50000` を白地に置く | **4.85** ✅（ゲートが見ているのはこれ） |
| それを `opacity-80` の中に入れる | **4.18** ❌（画面に出ているのはこれ） |

`opacity-80` という下限は `--text`（余裕が 12:1 以上ある）向けに決めたものだった。
**`--primary-d` は 4.5 のすぐ上を狙って作った色なので、80% を掛けると割る。**
片方だけを見ている検査は、両方を掛け合わせた結果を見落とす。

**→ ゲートに実ブラウザの走査を足すのが本筋。** 今回書いた測定ツールはそのまま使える。

---

## 実測でつまずいた2点（測りかた自体の知見・§7 への追記）

### ① `framenavigated` を数えると「初回に勝手にリロードした」と誤報する

§7-5 は E8 の測りかたを「`framenavigated` を数える（**1回なら正常**）」としている。
そのとおりに測ると Qalc は **2回**になり、いったん ❌ と判定した。**これは誤報だった。**

`framenavigated` は `history.pushState()` でも飛ぶ。
Qalc は端末の「戻る」でモーダルを閉じるために `BackNavigation.jsx:87` で `pushState` している。

| 測りかた | 結果 |
|---|---|
| `framenavigated` の回数 | 2 ← **誤報** |
| `performance` の navigation type | `["navigate"]` のみ |
| **文書が読みこまれた回数**（`addInitScript` の実行回数） | **1** ← 正しい |

**「戻る」をきちんと作ってあるアプリほど誤報が出る。** 数えるべきは画面遷移ではなく**文書の読みこみ**である。

### ② アニメーションの途中で測ると数字が変わる

画面を切りかえて 600ms で測ったときと、2,200ms 待ってから測ったときで**結果が食い違った。**
framer-motion の入場アニメーションが終わっていないと、まだ出ていない要素を数え落とす。

同じ理由で、**総当たりで押して回る探索も信用できなかった。**
モーダルが開いたままの画面と本来の画面を「同じ画面」と判定して、
`スコアアタック（したく）`のタップ不足を **11件 → 0件**に取りこぼしていた。
**画面ごとにまっさらから入り直す**形に変えて、ようやく数が安定した。

---

## v5 監査表

判定：✅ 適合 ／ ⚠️ 部分適合 ／ ❌ 未対応 ／ — 対象外 ／ 未計測

### A. 法務・配布
| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT・Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | `node_modules/` `dist/` `.env` ほか |
| A3 | dependabot.yml | ✅ | monthly |
| A4 | README / MANUAL / AUDIT | ✅ | 3点そろっている |

### B. セキュリティ
| B1 | CSP（入れたうえで動作確認ずみ） | ✅ | 全13画面で違反 0件 |
| B2 | 秘密情報・IDの直書きなし | ✅ | ゲート合格 |
| B3 | OAuthスコープ最小 | — | GAS を使っていない |
| B4 | postMessage の宛先が `*` でない | ✅ | ゲート合格 |
| B5 | サーバー側5段ガード | — | サーバーを持たない（P2P のみ） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **0 バイト** |
| B7 | 残る外部資産に SRI と版の固定 | — | 外部資産なし |

### C. 堅牢性
| C1 | LockService + try/finally | — | GAS ではない |
| C2 | 自動復旧 | — | シートを持たない |
| C3 | pagehide で記録確定 | ✅ | ゲート合格 |
| C4 | 通信失敗時のリトライと明示 | ⚠️ | 自動再接続は入れない判断（README に理由） |
| C5 | localStorage.clear() を使っていない | ✅ | ゲート合格 |

### D. 表示（Part I §2）
| D1 | viewport に viewport-fit=cover | ✅ | `index.html` / `offline.html` とも一致 |
| D2 | 100dvh を使用 | ✅ | `100vh` は `@supports` のフォールバックのみ |
| D3 | safe-area-inset を適用 | ✅ | 10か所 |
| D4 | clamp() による fluid type | ✅ | `--fs-*` を定義 |
| D5 | Canvas に DPR 補正（上限2） | ✅ | `Math.min(dpr, 2)`・解像度変化にも追従 |
| D6 | **320px 幅で横スクロールが出ない** | ✅ | **0件**（11ルート） |
| D7 | 画像に width/height、150KB以下 | ✅ | 最大 36.5KB |
| D8 | **コントラスト 4.5:1 以上** | ❌ | **110件**（原因4つ） |
| D9 | **タップ領域 44px 以上** | ❌ | **159件**（原因5つ）＋どうぐ画面に3種 |
| D10 | prefers-reduced-motion（`.01ms`） | ✅ | `0` ではない |
| D11 | forced-colors 対応 | ✅ | あり |
| D12 | 提示モード | ✅ | 実測で動作を確認 |
| D13 | 印刷CSS | — | 印刷して配る機能を持たない（判断は下記） |
| D14 | **拡大を禁止していない** | ✅ | 0件 |

### E. PWA（Part I §3）
| E1 | manifest の id/scope/start_url が絶対パス | ✅ | 3つとも `/Qalc/` |
| E2 | アイコン4種＋**透明を含まない** apple-touch-icon | ✅ | **透明 0%** |
| E3 | beforeinstallprompt を head 最上部の外部ファイルで捕捉 | ✅ | `<head>` の最初の `<script>` |
| E4 | インストールボタン（案内できるときだけ） | ✅ | `isStandalone` で出し分け |
| E5 | **自アプリ接頭辞のキャッシュのみ削除** | ✅ | **よそのキャッシュ2件が残ることを実測** |
| E6 | sw.js が localStorage に触れていない | ✅ | 0件 |
| E7 | **更新通知（押すまで切りかわらない）** | ✅ | **3秒放置で `waiting` のまま** |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | **文書の読みこみ 1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `getRegistration()` で active |
| E10 | **offline.html（外部資産・JS に頼らない）** | ❌ | インライン `<script>` に依存 |
| E11 | APP_VERSION を更新した | 未計測 | 今回の変更に合わせて上げる（未実施） |
| E12 | **maskable のセーフゾーン外 0.2% 以下** | ✅ | **0.00%**（192・512とも） |
| E13 | iOS のホーム画面追加手順を MANUAL に記載 | ✅ | ゲート合格 |

### F. アクセシビリティ・性能
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | 更新通知は `role="status"` |
| F2 | モーダルに role="dialog"・Esc で閉じる | ⚠️ | 主要導線は確認ずみ。全画面の網羅は**未計測** |
| F3 | キーボードのみで全機能に到達 | 未計測 | Tab 順の全画面検証は未実施（v4 から持ちこし） |
| F4 | **rt の色を決め打ちしていない** | ✅ | 指定なし＝継承。**実測の最小比 4.85** |
| F5 | 初回JS 300KB以下 | ❌ | **666.3KB**（gzip 204.5KB）。**天井 615.2KB も超えており CI が赤い** |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 `App.jsx` 3,900行 / 263KB |

### G. 学習ログ
| G1 | study.v1 準拠・個人情報を持たない | ✅ | ゲート合格 |
| G2 | 中断記録・5分ルール | ✅ | ゲート合格 |
| G3 | ロジック版の明記 | ✅ | 1.1 |
| G4 | 変わってはいけない値 | ✅ | 上限500件・設問200件・study.v1・誤答12文字 |

---

## 対応フェーズの割り当て（提案）

| フェーズ | 内容 | 触るもの | 破壊リスク |
|---|---|---|---|
| **P0** | **CI を緑に戻す**（F3 の天井） | `quality.config.json` | なし |
| **P1-a** | レアリティのバッジ 97件（塗りは変えず文字を濃く） | `shop.js` / `App.jsx` | 低 |
| **P1-b** | ランクの色 5件（色みは変えず明るさだけ） | `App.jsx:427` | 低 |
| **P1-c** | `opacity` の下限もれ（ON/OFF・`?`・`--primary-d`） | `presentation.jsx` / `App.jsx` | 低 |
| **P1-d** | タップ 44px（`.tap-44` を当てる。スライダーは別手当て） | `App.jsx` / `LearningTools.jsx` / `index.css` | 中（詰め組みの折り返しに注意） |
| **P1-e** | `offline.html` を JS なしに（＋CSP を入れる） | `offline.html` / `quality.config.json` | 低（`online` 自動復帰を失う） |
| **P3** | SW 登録に `readyState` 分岐（先回りの保険） | `pwa.jsx` | なし |
| **P4** | **測定ツールをリポジトリに入れ、ゲートに繋ぐ** | `tools/` / `check-*.mjs` | なし |

**P1 の各項目は独立している。** Part III「1つのPRに1つの目的だけ」に従い、分けて出せる。

## 人間に判断してほしいこと

1. **`offline.html` の `online` 自動復帰を捨ててよいか。**
   v5 §3-4 は JS を認めていない。捨てれば例外が1つ減るが、
   「つながり直したら勝手にもどる」気づかいは無くなる。

2. **F3（初回JS 666KB）をどうするか。**
   Part I の目標は 300KB。いまは 666KB で、gzip 後は 204KB。
   天井を上げて赤を消すだけにするか、分割まで踏みこむか。
   （v4 の時点で「天井を設けて肥大化は止める」方針に合意ずみ）

3. **D13 印刷。** Qalc は遊ぶアプリなので印刷は要らないと判断したが、
   `きろく` を家庭に見せる使いかたがあるなら印刷CSSが要る。

---

## 実施後の状態（2026-08-04）

合意した範囲（P0 ＋ P1 全部 ＋ ゲート強化）をすべて実施した。

### 実測（前 → 後）

| 項目 | 前 | 後 |
|---|--:|--:|
| **コントラスト基準未満**（実ブラウザ・13画面） | **110件** | **0件** |
| **タップ44px未満**（実ブラウザ・13画面） | **159件** | **0件** |
| 圏外ページのコントラスト | 2件 | **0件** |
| CSP違反 | 0件 | 0件 |
| 横スクロール（375px / 320px） | 0件 | 0件 |
| ふりがなの最小比 | 4.85 | 4.85 |
| **PWA の挙動（9項目）** | 8合格1不合格 | **9合格0不合格** |
| **圏外で起動する** | ❌ まっ白 | ✅ 本編が出る |
| 品質ゲート | 43合格1不合格（**CI が赤**） | **45合格0不合格** |
| コントラストの検査 | 352件 | **708件** |
| `main` の CI | **failure** | 緑（このブランチで確認） |

### 途中で見つかった、監査時には分かっていなかったもの

測定を進めるうちに、Phase 0 では見えていなかったものが3つ出た。

#### 1. 1回しか開いていない端末が圏外でまっ白になる（いちばん重い）

先読みの一覧に本体の JS/CSS が入っていなかった。
はじめて開いたときの `<script>` / `<link>` は Service Worker が管理下に入る前に
取りにいくので `fetch` のハンドラを素通りし、runtime キャッシュに1件も入らない。

```
qalc-cache-runtime-v1 が作られていない
assets/index-*.js   ERR_FAILED
assets/index-*.css  ERR_FAILED
```

2回目からは入るので、**手で試すと気づけない。**
Phase 0 でこの項目が ✅ だったのは、Service Worker がたまたま先に間に合った当たり回で、
同じ検査が回によって結果を変えていた。**通ったことより「なぜ通ったか」を見るべきだった。**

ファイル名にハッシュが付くので `sw.js` に手で書けない。ビルド時に書きこむようにした。

#### 2. 圏外ページのコントラストを一度も測っていなかった

画面の一覧に入れていなかったので走査の対象外だった。
測ると「もういちど ためす」が **2.78**、案内文が **3.82** だった。
**いま困っている人に向けた画面が、いちばん測られていなかった。**

#### 3. 手で選んだ色を、入れたばかりのゲートが21件止めた

明るい面・暗い面の代表を `#ffffff` と `#111111` で選んでいた。
実際にいちばん条件が悪いのは **retro の `#faf6ee`** と **aurora の `#134e4a`** で、
その2つに対しては届いていなかった。ゲートを入れていなければ気づけなかった。

### 直しかたの方針（配色は変えていない）

| 対象 | やったこと |
|---|---|
| レアリティのバッジ 97件 | 塗りはそのまま、載せる文字を `#111111` に（2.15〜3.96 → 4.77〜8.79） |
| ランク・チーム・どうぐの色 | 明るい面むけ／暗い面むけの2つを持ち、面を見て選ぶ |
| `opacity` の掛け合わせ 3か所 | `opacity` を文字だけに寄せ、文字用の濃い色には掛けない |
| タップ 159件 | `.tap-44`（疑似要素で当たり判定だけ）。見た目は1pxも変わらない |
| 算数ブロックのマス | 詰めて並んでいるので疑似要素は使わず、マスそのものを 44px に |
| スライダー | 入れものを 44px、見た目の棒は 8px のまま |

**テーマの色・塗りの色は1つも変えていない。**

### なぜ1色では直せなかったか

テーマが32種あり `--panel` が白から `#111111` まである。

| | 相対輝度 |
|---|---|
| 白地で 4.5:1 を満たす色 | 0.1785 **以下** |
| `#111111` の上で 4.5:1 を満たす色 | 0.2025 **以上** |

範囲が重ならないので、両方で読める1色は**存在しない**。
Part I §2-8 の「一括置換は濃い面の上の文字を壊す」がこれにあたる。

### 品質ゲートに足したもの

| # | 検査 | わざと壊して確認 |
|---|---|---|
| — | 変数でない色（レアリティ・ランク・チーム・どうぐ）を全テーマで測る | 6通り すべて落ちた |
| E8c | 圏外で起動できる（先読みに本体が入っている） | 2通り すべて落ちた |

「0件でした」だけでは検査が動いているか分からないので、**8通り壊して8通りとも落ちること**を確認した。
実測ツール（`npm run measure`）のほうも、圏外ページの文字を薄くして終了コード1になることを確認した。

### 積み残し（「未計測」であって ✅ ではない）

| 項目 | 状態 |
|---|---|
| きろく画面 | 記録が無いと出ない。**未計測** |
| みんなであそぶ の本編・じんとり・ボスバトル | `wss://0.peerjs.com` へ出られない。入口までは実測ずみ。**本編は未計測** |
| 本番（`gigayama.github.io`）での挙動 | プロキシ 403。**未計測** |
| F3 初回JS 666.3KB | 目標 300KB には未達。天井を上げて赤を消す方針で合意ずみ |
| F2 Tab 順の全画面検証 | 主要導線のみ。**全画面は未計測** |
| D13 印刷CSS | 遊ぶアプリなので対象外と判断。`きろく` を家庭に見せる使いかたがあるなら要検討 |
